import 'package:flutter/material.dart';

class ChatAI_Tab extends StatelessWidget {
  const ChatAI_Tab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'AI Chat Screen',
        style: TextStyle(fontSize: 24, color: Colors.black),
      ),
    );
  }
}